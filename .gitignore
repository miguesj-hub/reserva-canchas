# Structurizr — diagramas/
diagramas/.structurizr/
diagramas/.chrome-profile/
diagramas/svg/
diagramas/node_modules/
structurizr-lite.war
structurizr-cli/

# workspace.json NO se ignora: guarda el layout de los diagramas.


# Credenciales locales — nunca versionar (taller §6.2)
*/.env
material-curso/
# macOS
.DS_Store


# Credenciales locales — nunca versionar (taller §6.2)
*/.env
material-curso/

# Red de seguridad a nivel de raíz. Cada proyecto Maven y cada paquete npm
# tiene además su propio .gitignore.
target/
**/target/
*.class
node_modules/
**/node_modules/
dist/
**/dist/

# Credenciales locales — tampoco en la raíz
.env
.env.*
!.env.example

# Material de referencia del curso — se queda en local, no se versiona.
Alcance_Funcional_Reserva_Canchas_v2.md
Spring_Boot_Microservicios.md

# Configuración del IDE, propia de cada máquina.
.idea/
